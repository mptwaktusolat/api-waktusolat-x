services:
  app:
    build:
      context: .
      dockerfile: .docker/production/Dockerfile
    restart: unless-stopped
    ports:
      - '8080:8080'
    environment:
      - DB_HOST=host.docker.internal
    env_file:
      - .env